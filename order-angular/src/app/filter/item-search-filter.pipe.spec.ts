import { ItemSearchFilterPipe } from './item-search-filter.pipe';

describe('ItemSearchFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ItemSearchFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
